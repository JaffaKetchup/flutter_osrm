import 'package:flutter/material.dart';
import 'package:flutter_map/flutter_map.dart';
import 'package:latlong2/latlong.dart';

class RoutingController {}
/*class RoutingLayer extends LayerOptions {
  List<LatLng> _waypoints = [];
  List<LatLng> _nodes = [];

  PolylineLayerOptions create(dynamic newController) {
    newController = RoutingController();
    return PolylineLayerOptions(
      polylines: [
        Polyline(
          points: _nodes,
          strokeWidth: 5,
          color: Colors.black,
          borderColor: Colors.white,
          borderStrokeWidth: 5,
        ),
      ],
    );
  }

  void addWaypoints() {}
}*/
